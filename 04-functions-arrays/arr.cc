// Deepak Aggarwal, Coding Blocks
// deepak@codingblocks.com
#include <iostream>
using namespace std;
int main() {
    // int arr[5];                    // array defination/declaration
    int arr[5] = {1, 2, 3 , 4, 5}; // array initialisation
    int n; cin >> n;

    
    // cout << arr[0] << endl;
    // cout << arr[4] << endl;

    cout << arr[5] << endl;

    // cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    // cout << arr[0] << arr[1] << arr[2] << arr[3] << arr[4];

    // for (int i = 0; i < 5; ++i) {
    //     cin >> arr[i];
    // }

}